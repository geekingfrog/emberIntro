* basic setup with application template
* add route for cakes + model
* introduce {{ outlet }} helper
* add route for individual cake and cakes/index template
* redirect from index
* bindAttr for image
* {{ linkTo }} helper
* {{ render }} helper
* {{ action }} and 'needs'
* computed property on array with @each
* handlebars helper
